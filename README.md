# DiscordChatBot

Dependencies:

npm i discord.js

npm i enmap

npm i enmap-levels

npm i tmi.js



Setup:

use the config.example and create a config.json with the info there, but fill in the details needed
